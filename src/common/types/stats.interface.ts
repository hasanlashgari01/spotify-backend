export interface PlaylistStats {
  totalDuration: string;
  songCount: string;
}

export interface GenreStats {
  id: number;
  title: string;
  slug: string;
  cover: string;
  songCount: string;
  totalDuration: string;
}