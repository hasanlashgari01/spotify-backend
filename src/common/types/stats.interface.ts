export interface PlaylistStats {
  totalDuration: string;
  totalCount: string;
}

export interface GenreStats {
  id: number;
  title: string;
  slug: string;
  cover: string;
  totalCount: string;
  totalDuration: string;
}