export enum Role {
    USER = "user",
    ADMIN = "admin",
    ARTIST = "artist",
}

export enum Status {
    PUBLIC = "public",
    PRIVATE = "private",
}

export enum Gender {
    MALE = "male",
    FEMALE = "female",
    OTHER = "other",
}
